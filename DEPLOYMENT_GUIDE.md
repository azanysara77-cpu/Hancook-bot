# الدليل النهائي لنشر Hancock-bot على Render (بدون عرقلات)

هذه النسخة مخصصة بالكامل لتعمل على **Node.js** مباشرة (بدون Docker) ومبرمجة لترتبط برقمك **967730403327**.

## 🚀 نصائح ذهبية لضمان نجاح الرفع والربط

لضمان عمل البوت من المرة الأولى وبدون أي "تعذر في ربط الجهاز"، اتبع هذه النصائح بدقة:

| النصيحة | التفاصيل |
| :--- | :--- |
| **تحديث التطبيق** | تأكد أن تطبيق واتساب على هاتفك هو **أحدث إصدار** متاح في المتجر. |
| **استقرار الإنترنت** | يفضل استخدام **شبكة Wi-Fi** مستقرة أثناء عملية إدخال كود الاقتران. |
| **توقيت الكود** | بمجرد ظهور الكود في سجلات (Logs) موقع Render، أدخله في هاتفك **خلال أقل من دقيقة**. |
| **إعادة التشغيل** | إذا فشل الربط أول مرة، قم بـ **إغلاق تطبيق واتساب تماماً** من الخلفية ثم افتحه وحاول مجدداً. |
| **تجنب الحظر** | لا تقم بمحاولات ربط متكررة جداً (أكثر من 3 مرات في الساعة) لتجنب حظر الرقم مؤقتاً من قبل واتساب. |

## 🛠️ خطوات الرفع على Render

1.  **على GitHub:**
    *   قم بإنشاء مستودع (Repository) جديد.
    *   ارفع جميع ملفات المجلد (index.js, package.json, render.yaml, handler.js) إليه.
2.  **على Render:**
    *   اختر **New > Web Service**.
    *   اربط مستودع GitHub الخاص بك.
    *   سيقوم Render تلقائياً بقراءة ملف `render.yaml` وضبط الإعدادات (Node.js, Port 3000).
    *   اضغط **Create Web Service**.
3.  **عملية الربط:**
    *   انتظر حتى تظهر كلمة `Build Successful` في Render.
    *   اذهب إلى تبويب **Logs**.
    *   ستجد رسالة كبيرة باللون الأخضر تحتوي على **كود الاقتران**.
    *   في هاتفك: الإعدادات > الأجهزة المرتبطة > ربط جهاز > **الربط برقم الهاتف**.
    *   أدخل الكود الظاهر في Render.

## ⚠️ ملاحظة حول الاستمرارية
في خطة Render المجانية، يتم إيقاف الخدمة إذا لم تتلقَ زيارات (HTTP requests). البوت مبرمج ليرد على طلبات Render، ولكن لضمان بقائه يعمل 24 ساعة، يفضل استخدام خدمة مثل [Cron-job.org](https://cron-job.org/) لزيارة رابط موقعك في Render كل 10 دقائق.

---
*تم التجهيز بواسطة: Manus AI*
*الرقم المستهدف: 967730403327*
