# 🧊 Hancock Bot - WhatsApp Bot 🧊

بوت واتساب متطور مبني باستخدام مكتبة `@whiskeysockets/baileys` مع نظام ألعاب، بنك، وصلاحيات متكامل.

## ✨ المميزات
- **نظام بنكي:** إنشاء حسابات، إيداع، سحب، تحويل، واستثمار.
- **ألعاب وفعاليات:** (XO، ترتيب، تفكيك، كتابة، أعلام، احزر الإيموجي).
- **نظام متجر:** شراء ممتلكات متنوعة.
- **نظام صلاحيات:** التحكم في من يمكنه استخدام البوت.
- **دعم Render:** جاهز للتشغيل 24/7 مع خادم Express مدمج.

## 🚀 التشغيل السريع

### 1. المتطلبات
- Node.js 20+
- حساب GitHub
- حساب Render

### 2. التثبيت
```bash
npm install
```

### 3. التشغيل
```bash
npm start
```

## 🛠️ الإعداد للنشر (Render)
1. ارفع الكود إلى مستودع (Repository) خاص على GitHub.
2. اربط المستودع بـ Render كخدمة **Web Service**.
3. استخدم ملف `render.yaml` المرفق للإعداد التلقائي.
4. عند التشغيل لأول مرة، سيظهر **Pairing Code** في سجلات (Logs) الـ Render، استخدمه لربط البوت برقم الواتساب.

## 📝 ملاحظات
- تم إضافة ملف `.gitignore` لمنع رفع بيانات الجلسة والمستخدمين، مما يضمن أمان حسابك.
- البوت مهيأ ليعمل "كأنه جديد" بدون أي ذاكرة سابقة.

## 👤 المطور
**بوراشد**


## 🛡️ نصائح لتجنب الحظر (Anti-Ban Tips)
تم تحديث هذا البوت بتقنيات متقدمة لتجنب الحظر، ولكن يرجى اتباع الإرشادات التالية لضمان أقصى درجات الأمان:
1. **لا تستخدم البوت لإرسال رسائل سبام:** تجنب إرسال عدد كبير من الرسائل لأشخاص ليسوا في جهات اتصالك.
2. **التدرج في الاستخدام:** إذا كان الرقم جديداً، ابدأ باستخدامه بشكل بسيط قبل تفعيل البوت بشكل مكثف.
3. **التفاعل البشري:** حاول الرد يدوياً على بعض الرسائل من وقت لآخر من نفس الرقم.
4. **المجموعات:** البوت يعمل بشكل أفضل وأكثر أماناً داخل المجموعات مقارنة بالرسائل الخاصة المكثفة.
5. **التحديثات:** تأكد دائماً من استخدام أحدث إصدار من مكتبة Baileys.

*تمت إضافة ميزات (حالة الكتابة، الفواصل الزمنية العشوائية، محاكاة السلوك البشري) تلقائياً في هذا الإصدار.*
